<Window x:Class="MVVM_Base.View.CustomModal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="#DD333333"
        Width="300"
        SizeToContent="Height"
        MaxHeight="400"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Topmost="True"
        ShowActivated="True">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <TextBlock x:Name="MessageText" 
                   Grid.Row="0"
                   Foreground="White"
                   TextWrapping="Wrap" 
                   FontSize="16" 
                   TextAlignment="Center"
                   Margin="0 0 0 20"/>
        </ScrollViewer>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                Content="Yes" Width="80" HorizontalAlignment="Center"
                Click="OnOK"/>
            <Button Grid.Column="1"
                Content="No" Width="80" HorizontalAlignment="Center"
                Click="OnCancel"/>
        </Grid>

    </Grid>
</Window>
